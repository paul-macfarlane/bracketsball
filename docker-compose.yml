services:
  db:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: bracketiering
      POSTGRES_PASSWORD: bracketiering
      POSTGRES_DB: bracketiering
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
