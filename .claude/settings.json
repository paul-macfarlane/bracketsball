{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(jq -r '.tool_input.file_path // empty') && if [ -n \"$file_path\" ] && echo \"$file_path\" | grep -qE '\\.(ts|tsx|js|jsx|json|css|md|html)$'; then npx prettier --write \"$file_path\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
